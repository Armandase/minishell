faire des tokens mais si quote alors pas prendre en comtpe les tokens

ls|echo -n>out1>out2>out3 |"ls -la "|tr 'x' 'd' | grep "ls -a | cat"

tokens : >|<

faire token:
	ls
	echo -n
	out1
	out2
	out3
	"ls -la"
	tr 'x' 'd'-> split avec les espace pour execve	
	"ls -a | cat" -> si quote alors pas split avec les espaces

si quote ouverte et pas fermé:
	si meme quote (' ou ") est pas fermé dans un autre token
		alors le token courant = concatenation jusqu'a la 
		fin du token de la prochaine quote
	else la quote est ascii

	ls "-la" "ok" "str" "open | and close" ls
	
grep " pipex | heredoc " --with-filename

grep " pipex 
heredoc " --with-filename

grep
pipex heredoc
--with-filename

end"ok"oui
 GERER SI UN DELIM AU DEBUT (> ls ou < ou >> okkofsdkfds)
 



